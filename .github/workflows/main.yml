Skip to content
rapriyan1204-ux
verified-agent-identity
Repository navigation
Code
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Verify Human Identity
Verify Human Identity #1
All jobs
Run details
Workflow file for this run
.github/workflows/main.yml at 13bc4b4
name: Verify Human Identity

on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'apriyan1204'
        required: true
        default: 'apriyan1204'
      agent_description:
        description: 'apriyan1204'
        required: true
        default: 'apriyan1204 AI Agent'

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Step 1 - Install ClawHub skill
        run: npx clawhub@latest install verified-agent-identity

      - name: Install script dependencies
        run: |
          cd scripts
          npm install
      - name: Step 2 - Create new Ethereum Identity
        run: node scripts/createNewEthereumIdentity.js

      - name: Step 3 - Link Human to Agent
        run: |
          node scripts/manualLinkHumanToAgent.js --challenge '{"name": "apriyan1204", "description": "apriyan1204 AI Agent"}'
Create main.yml · rapriyan1204-ux/verified-agent-identity@13bc4b4
